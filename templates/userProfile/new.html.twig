{% extends 'base.html.twig' %}

{% block title %}New UserProfile{% endblock %}

{% block body %}
    <h1>Create new UserProfile</h1>

    {{ include('userProfile/_form.html.twig') }}

    <a class="btn btn-primary"href="{{ path('userProfile_index') }}">back to list</a>
{% endblock %}
