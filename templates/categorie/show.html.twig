{% extends 'base.html.twig' %}

{% block title %}Categorie
{% endblock %}

{% block body %}
	<div class="show container ">
		<h1>Categorie</h1>
	</div>
	<div class="container">
		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ categorie.id }}</td>
				</tr>
				<tr>
					<th>Name</th>
					<td>{{ categorie.name }}</td>
				</tr>
				<tr>
					<th>Name</th>
					<td>{{ categorie.firstname }}</td>
				</tr>
				<tr>
					<th>Name</th>
					<td>{{ categorie.lastname }}</td>
					<tr>
					<th>Name</th>
					<td>{{ categorie.title }}</td>
				</tr>
				</tr>
			</tbody>
		</tr>
	</tr>

</tbody></table><a class="btn btn-primary" href="{{ path('categorie_index') }}">back to list</a><a class="btn btn-primary" href="{{ path('categorie_edit', {'id': categorie.id}) }}">edit</a>{{ include('categorie/_delete_form.html.twig') }}</div>{% endblock %}
