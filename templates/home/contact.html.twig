{% extends 'base.html.twig' %}

{% block title %}contact{% endblock %}

{% block body %}
	<div class="container">
		<h1>Contact space</h1>
        {{form_start(form)}}
        {{form_widget(form)}}
        <button class="btn btn-info text-dark mb-5">Submit</button>
        <a class="btn btn-primary mb-5" href="{{path ("home")}}">Accueil</a>
        {{form_end(form)}}
	</div>
    {% endblock %}
